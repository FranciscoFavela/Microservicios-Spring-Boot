server:
  port: 8081

spring:
  cloud:
    config:
      server:https://github.com/FranciscoFavelaNajera/Microservicios.git
        git:
          uri:
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t